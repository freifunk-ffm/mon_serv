<h1>Listing nodes</h1>

<table>
  <tr>
	<th>id</th>
	<th>mac</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @nodes.each do |node| %>
  <tr>
	<td><%= node.id%></td>
	<td><%= node.id.to_s(16)%>
	<td><%= link_to 'Stats', node_stats_path(node)%>
    <td><%= link_to 'Show', node %></td>
    <td><%= link_to 'Destroy', node, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	<td>
		<img src="/nodes/110432246001710/stats/ping.png" />
	</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Node', new_node_path %>
